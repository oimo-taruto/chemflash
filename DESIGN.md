# ChemFlash 設計メモ（壁打ちで決めた仕様）

対象: 阪大薬学志望の高三生が受験まで毎日使う前提。

## 基本方針
「一度きりの暗記チェック」ではなく「入試までの定着管理ツール」。
演習で何周もする楽しさは残しつつ、忘却曲線で定着を底上げする。

## 自己評価は3段階
`🙅わからない（ng） / 🤔あいまい（vague） / 🙆完璧（ok）`

- 「あいまい」（なんとなく解けたが不安）は本番で落とす最重要ゾーン。2択だと「完璧」に押し込まれて定着が嘘になるので3段階にした。
- **集めるのは3段階／判定は2値／見せ方だけ3色**: スケジューラ（忘却曲線）は3段階をフルに使うが、分析の指標定義は増やさない。各バーを3色積み上げにして情報だけ増やす。

## 忘却曲線（Leitner式・3回で卒業）
段階アップは **期限が来た問題に「完璧」をつけたときだけ**、かつ **1日1回まで**（演習を同日に何周しても駆け抜けない）。

```
完璧①              → 1週間後に「今日の復習」へ
完璧②（1週間後）    → 1ヶ月後へ
完璧③（1ヶ月後）    → 🎓卒業（今日の復習から外れる）
あいまい            → 段階そのまま・最短(1週間)へ
わからない          → 段階リセット・最短(1週間)へ ＋ 間違い復習へ
```

- 短い間隔（翌日）は不要。「今日中の粘り」は **間違い復習** が担当し、**忘却曲線は長い間隔だけ**に専念する役割分担。
- 🎓卒業 ≠ 二度と出ない。演習の周回には出続けるし、演習で「わからない」を踏むと自動で復活して復習キューに戻る。
- 間隔 `[7, 30]` 日は [js/store.js](js/store.js) の `SRS_INTERVALS` で調整可能。

## 復習ゾーンは2本立て（演習・既存機能は不変）
| | 対象 | 役割 |
|---|---|---|
| 🔁 間違い復習 | 完璧以外（ng・vague） | 今すぐ粘って潰す |
| 📅 今日の復習 | 忘却曲線の期限到来（卒業前） | 日をまたいだ維持 |

演習モードはUI不変で、答えた結果を裏で忘却曲線に流すだけ。

## 分析指標（3つ・定義はシンプルに固定）
- **初回正答率**: 初回が「完璧」だった割合（あいまい・わからないは不正解扱い）
- **理解度**: 解いた問題のうち最新が「完璧」の割合
- **カバー率**: （未習を除く）全問のうち1回でも解いた割合
- 未習は分母から除外。件数(n)も併記。🎓定着はカウント表示のみ（新指標は増やさない）。

## 未習（①）
「まだ授業で習っていない」問題を出題・統計から除外。書庫でいつでも解除。解答すると自動で「習った」扱いになる。

**学習範囲のまとめ設定**: 書庫の先頭に「🎚 学習範囲」パネル。単元・分野ごとにワンタップで未習⇄学習中を一括切替（`setUnlearnedScope(unit, sub, on)`）。状態は3つ — ✓習った / ⤳全部未習 / –一部未習(partial)。1問ずつの未習ボタンも併存（例外用）。授業の進度に合わせて分野を解禁していく運用を想定。

## 公式問題の配信と自作問題（origin）
各問題は `origin: 'official' | 'custom'` を持つ。

- **official（公式）**: アプリ内蔵の `js/seed.js` が原本。起動時に `syncOfficialQuestions` が端末の公式問題を最新版へ揃えるため、push だけで全端末に配信される。id は問題文のハッシュなので、答え・タグ等だけの修正は進捗を引き継ぐ（問題文の変更はリセット）。
- **custom（自作）**: 生徒が保存タブで作る個人の問題。配信同期は一切触らない。unit は「自作問題」固定で、分析では独立タイルになる。
- 端末側で公式を編集すると custom にフォーク（配信で巻き戻さない）。配布版が同じ内容に追いつけば公式へ自動で一本化。削除した公式は `removedOfficial`（墓標）に記録し、配信で復活させない。

管理画面は先生の作問デスク（アプリからのリンクは無し）。配布フローは「admin.html でCSVエクスポート → Claude Code が SEED に焼き込み → push」。

## まだ入れていない（今後の候補）
今日のノルマ／連続日数、入試日カウントダウン、能動的想起（思い出してから／入力モード）、化学式のKaTeX表示、理由型と丸暗記でスケジュールを変える、理解度の推移グラフ。
